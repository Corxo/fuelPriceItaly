version: '3.9'

services:
  bot:
    container_name: 'oilPrice'
    image: oilprice
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    volumes:
      - oilPriceDb:/db/

volumes:
  oilPriceDb: